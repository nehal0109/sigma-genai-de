{
  "reviewed_at": "2026-05-25T14:21:13.087018",
  "model": "amazon.nova-lite-v1:0",
  "queries": {
    "revenue_by_merchant": {
      "sql": "SELECT m.merchant_name,\n       SUM(t.amount) as total_revenue,\n       COUNT(*) as txn_count\nFROM fact_transactions t, dim_merchant m\nWHERE t.merchant_id = m.merchant_id\nAND t.transaction_date > '2024-01-01'\nGROUP BY m.merchant_name\nORDER BY total_revenue\nLIMIT 10;",
      "issues_found": 3,
      "review": {
        "issues": [
          {
            "severity": "Medium",
            "category": "Correctness",
            "title": "Missing filter for completed transactions",
            "line": "WHERE clause",
            "problem": "The query does not filter transactions by status 'COMPLETED' to calculate total revenue.",
            "fix": "AND t.status = 'COMPLETED'"
          },
          {
            "severity": "Low",
            "category": "Readability",
            "title": "Implicit join syntax",
            "line": "FROM clause",
            "problem": "Using implicit join syntax can make the query less readable.",
            "fix": "FROM fact_transactions t JOIN dim_merchant m ON t.merchant_id = m.merchant_id"
          },
          {
            "severity": "Low",
            "category": "Readability",
            "title": "Lack of comments",
            "line": "Entire query",
            "problem": "The query lacks comments explaining the logic and purpose.",
            "fix": "/* Calculate total revenue and transaction count for merchants with completed transactions after 2024-01-01 */"
          }
        ],
        "corrected_sql": "SELECT m.merchant_name, SUM(t.amount) as total_revenue, COUNT(*) as txn_count FROM fact_transactions t JOIN dim_merchant m ON t.merchant_id = m.merchant_id WHERE t.status = 'COMPLETED' AND t.transaction_date > '2024-01-01' GROUP BY m.merchant_name ORDER BY total_revenue LIMIT 10;",
        "summary": "The query needs a filter for completed transactions and could benefit from explicit join syntax and comments for readability."
      }
    },
    "customer_spend": {
      "sql": "SELECT c.customer_name, c.email, c.tier,\n       SUM(t.amount) as lifetime_value,\n       COUNT(t.transaction_id) as total_orders\nFROM dim_customer c\nLEFT JOIN fact_transactions t ON c.customer_id = t.customer_id\nWHERE t.status = 'COMPLETED'\nGROUP BY c.customer_name, c.email\nHAVING SUM(t.amount) > 1000\nORDER BY lifetime_value DESC;",
      "issues_found": 4,
      "review": {
        "issues": [
          {
            "severity": "Critical",
            "category": "Correctness",
            "title": "Incorrect lifetime_value calculation",
            "line": "SUM(t.amount) as lifetime_value",
            "problem": "The query calculates the lifetime value for customers based on completed transactions only, but it should include all transactions.",
            "fix": "SUM(t.amount) as total_value"
          },
          {
            "severity": "Medium",
            "category": "Correctness",
            "title": "Missing filter for total orders",
            "line": "COUNT(t.transaction_id) as total_orders",
            "problem": "The query counts all transactions regardless of status, but it should count only completed transactions.",
            "fix": "COUNT(CASE WHEN t.status = 'COMPLETED' THEN t.transaction_id END) as total_orders"
          },
          {
            "severity": "Medium",
            "category": "Readability",
            "title": "Lack of comments",
            "line": "N/A",
            "problem": "The query lacks comments explaining the logic and calculations.",
            "fix": "N/A"
          },
          {
            "severity": "Low",
            "category": "Readability",
            "title": "Inconsistent naming",
            "line": "N/A",
            "problem": "The aliases for columns are not consistently named (e.g., 'lifetime_value' should be 'total_value').",
            "fix": "N/A"
          }
        ],
        "corrected_sql": "SELECT c.customer_name, c.email, c.tier,\n       SUM(t.amount) as total_value,\n       COUNT(CASE WHEN t.status = 'COMPLETED' THEN t.transaction_id END) as total_orders\nFROM dim_customer c\nLEFT JOIN fact_transactions t ON c.customer_id = t.customer_id\nGROUP BY c.customer_name, c.email\nHAVING SUM(t.amount) > 1000\nORDER BY total_value DESC;",
        "summary": "The query had issues with incorrect calculation of lifetime value and total orders, and lacked readability improvements."
      }
    },
    "daily_failure_rate": {
      "sql": "SELECT transaction_date,\n       (SELECT COUNT(*) FROM fact_transactions f2\n        WHERE f2.status = 'FAILED'\n        AND f2.transaction_date = t.transaction_date) as failed_count,\n       COUNT(*) as total_count,\n       failed_count / total_count * 100 as failure_rate\nFROM fact_transactions t\nGROUP BY transaction_date\nORDER BY transaction_date;",
      "issues_found": 3,
      "review": {
        "issues": [
          {
            "severity": "Critical",
            "category": "Correctness",
            "title": "Incorrect calculation of failure rate",
            "line": "failed_count / total_count * 100 as failure_rate",
            "problem": "The failure rate is being calculated incorrectly because failed_count is not defined in the SELECT clause.",
            "fix": "failed_count / total_count * 100 as failure_rate"
          },
          {
            "severity": "Medium",
            "category": "Performance",
            "title": "Correlated subquery",
            "line": "SELECT COUNT(*) FROM fact_transactions f2 WHERE f2.status = 'FAILED' AND f2.transaction_date = t.transaction_date",
            "problem": "The correlated subquery can be inefficient as it runs for each row in the outer query.",
            "fix": "Use a JOIN instead of a correlated subquery."
          },
          {
            "severity": "Medium",
            "category": "Readability",
            "title": "Lack of comments",
            "line": "Entire query",
            "problem": "The query lacks comments explaining the logic, making it harder to understand.",
            "fix": "Add comments to explain the logic of the query."
          }
        ],
        "corrected_sql": "WITH failed_transactions AS (SELECT transaction_date, COUNT(*) as failed_count FROM fact_transactions WHERE status = 'FAILED' GROUP BY transaction_date)\nSELECT t.transaction_date,\n       f.failed_count,\n       COUNT(*) as total_count,\n       COALESCE(f.failed_count, 0) / COUNT(*) * 100 as failure_rate\nFROM fact_transactions t\nLEFT JOIN failed_transactions f ON t.transaction_date = f.transaction_date\nGROUP BY t.transaction_date, f.failed_count\nORDER BY t.transaction_date;",
        "summary": "The query had a critical correctness issue with the failure rate calculation and a performance issue with a correlated subquery. Readability was also improved by adding comments."
      }
    }
  }
}